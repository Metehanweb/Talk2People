export const ROLES = {
    USER: 'user',
    MODERATOR: 'moderator',
    ADMIN: 'admin',
};

export const ALL_ROLES = Object.values(ROLES);

export const EXTRA_ROLES = {
    VIP: 'vip',
    SUPPORT: 'support',
    FOUNDER: 'founder',
    TESTER: 'tester',
};

export const ALL_EXTRA_ROLES = Object.values(EXTRA_ROLES);
